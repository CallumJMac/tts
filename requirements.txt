# Requirements for tts-fewshot experiment
# Only direct dependencies are pinned; transitive deps resolved by pip.
#
# NOTE: torch and torchaudio are NOT listed here.
# They must be installed separately with the correct index URL:
#   CPU:  pip install torch==2.10.0 torchaudio==2.10.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.10.0 torchaudio==2.10.0 --index-url https://download.pytorch.org/whl/cu126
#   MPS:  pip install torch==2.10.0 torchaudio==2.10.0  (default PyPI, macOS only)
#
# Optional (CUDA-only): pip install flash-attn --no-build-isolation
# Optional (SpeechBERTScore): pip install "discrete-speech-metrics @ git+https://github.com/Takaaki-Saeki/DiscreteSpeechMetrics.git@350e19062839029a66d72541312852ca12c7f1b0"

# === Core TTS ===
qwen-tts==0.1.1
transformers==4.57.3
accelerate==1.12.0
safetensors==0.7.0
sentencepiece==0.2.1

# === Audio ===
librosa==0.11.0
soundfile==0.13.1
pydub==0.25.1

# === Evaluation ===
openai-whisper==20250625
jiwer==3.1.0

# === Data / Experiment ===
numpy==2.3.5
pandas==3.0.0
scipy==1.17.0
scikit-learn==1.8.0
tqdm==4.67.3

# === Utilities ===
pyyaml==6.0.3
requests==2.32.5
huggingface_hub==0.36.2
tokenizers==0.22.2
tiktoken==0.12.0
regex==2026.1.15
